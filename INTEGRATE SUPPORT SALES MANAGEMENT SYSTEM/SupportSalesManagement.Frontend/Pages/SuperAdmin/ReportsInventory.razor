@page "/superadmin/reports/inventory"
@layout SuperAdminLayout
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/superadmin/reports/financial", replace: true);
    }
}

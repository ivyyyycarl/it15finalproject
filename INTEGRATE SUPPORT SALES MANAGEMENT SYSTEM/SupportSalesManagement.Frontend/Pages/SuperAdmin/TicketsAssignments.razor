@page "/superadmin/tickets/assignments"
@layout SuperAdminLayout
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/superadmin/tickets/all", replace: true);
    }
}

@page "/superadmin/help/knowledge-base"
@layout SuperAdminLayout
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/superadmin/dashboard", replace: true);
    }
}
